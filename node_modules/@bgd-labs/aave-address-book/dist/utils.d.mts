import { Address } from 'viem';

/**
 * Checks if address is listed on address-book
 * @param value
 * @param chainId
 * @returns string[] found paths to address-book addresses
 */
declare function getAddressBookReferences(value: Address, chainId: number): string[];

export { getAddressBookReferences };
