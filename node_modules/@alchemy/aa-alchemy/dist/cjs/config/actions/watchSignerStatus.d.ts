import type { SignerStatus } from "../store/types.js";
import type { AlchemyAccountsConfig } from "../types";
export declare const watchSignerStatus: (config: AlchemyAccountsConfig) => (onChange: (status: SignerStatus) => void) => () => void;
