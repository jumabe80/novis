import type { AlchemySigner } from "../../signer";
import type { AlchemyAccountsConfig } from "../types";
export declare const watchSigner: (config: AlchemyAccountsConfig) => (onChange: (signer?: AlchemySigner) => void) => () => void;
