import type { AuthStep } from "../../context";
type LoadingAuthProps = {
    context?: AuthStep;
};
export declare const LoadingAuth: ({ context }: LoadingAuthProps) => import("react/jsx-runtime").JSX.Element | null;
export {};
