import { useConnect as wagmi_useConnect, } from "wagmi";
import { useAlchemyAccountContext } from "../context.js";
export const useConnect = (params) => {
    const { config: { _internal: { wagmiConfig }, }, } = useAlchemyAccountContext();
    return wagmi_useConnect({
        config: wagmiConfig,
        mutation: params,
    });
};
//# sourceMappingURL=useConnect.js.map