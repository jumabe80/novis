import { useSyncExternalStore } from "react";
import { getConnection } from "../../config/actions/getConnection.js";
import { watchConnection } from "../../config/actions/watchConnection.js";
import { useAlchemyAccountContext } from "../context.js";
export function useConnection() {
    const { config } = useAlchemyAccountContext();
    return useSyncExternalStore(watchConnection(config), () => getConnection(config), () => getConnection(config));
}
//# sourceMappingURL=useConnection.js.map