"use client";
import { useSyncExternalStore } from "react";
import { getSignerStatus } from "../../config/actions/getSignerStatus.js";
import { watchSignerStatus } from "../../config/actions/watchSignerStatus.js";
import { useAlchemyAccountContext, } from "../context.js";
export const useSignerStatus = (override) => {
    const { config } = useAlchemyAccountContext(override);
    return useSyncExternalStore(watchSignerStatus(config), () => getSignerStatus(config), () => getSignerStatus(config));
};
//# sourceMappingURL=useSignerStatus.js.map