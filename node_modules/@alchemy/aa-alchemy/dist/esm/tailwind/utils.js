export const createColorSet = (light, dark) => ({
    light,
    dark,
});
export const apply = (source, target) => {
    if (!target)
        return source;
    for (const key in source) {
        if (target[key] == null) {
            continue;
        }
        else if (typeof source[key] !== "object") {
            source[key] = target[key];
        }
        else {
            apply(source[key], target[key]);
        }
    }
    return source;
};
export const getColorVariableName = (name) => {
    return `--akui-${name}`;
};
//# sourceMappingURL=utils.js.map