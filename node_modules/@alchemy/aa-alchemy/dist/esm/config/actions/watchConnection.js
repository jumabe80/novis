export function watchConnection(config) {
    return (onChange) => {
        return config.coreStore.subscribe(({ chain }) => chain, (chain) => {
            const connection = config.coreStore
                .getState()
                .connections.get(chain.id);
            if (connection) {
                onChange(connection);
            }
        });
    };
}
//# sourceMappingURL=watchConnection.js.map