export const watchUser = (config) => (onChange) => {
    return config.clientStore.subscribe(({ user }) => user, onChange, {
        equalityFn: (a, b) => a?.userId === b?.userId,
    });
};
//# sourceMappingURL=watchUser.js.map