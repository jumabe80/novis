import type { Chain } from "viem";
import type { AlchemyAccountsConfig } from "../types";
export declare function watchChain(config: AlchemyAccountsConfig): (onChange: (chain: Chain) => void) => () => void;
