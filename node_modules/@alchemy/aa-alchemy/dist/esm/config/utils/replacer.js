export const bigintMapReplacer = (_key, value_) => {
    let value = value_;
    if (typeof value === "bigint")
        value = { __type: "bigint", value: value_.toString() };
    if (value instanceof Map)
        value = {
            __type: "Map",
            value: Array.from(value_.entries()),
        };
    return value;
};
//# sourceMappingURL=replacer.js.map