"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ContractAddressesGenPhase = void 0;
const dedent_1 = __importDefault(require("dedent"));
const ContractAddressesGenPhase = async (input) => {
    const { contract, content, addImport } = input;
    addImport("viem", { name: "Address", isType: true });
    content.push((0, dedent_1.default) `
    const addresses = {${Object.entries(contract.address).reduce((prev, [chainId, addr]) => (prev += `${chainId}: "${addr}" as Address, `), "")}} as Record<number, Address>;
  `);
    return input;
};
exports.ContractAddressesGenPhase = ContractAddressesGenPhase;
//# sourceMappingURL=contract-addresses-gen.js.map