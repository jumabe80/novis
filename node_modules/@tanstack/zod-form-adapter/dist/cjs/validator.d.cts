import { ZodType } from 'zod';
import { ValidationError } from '@tanstack/form-core';

export declare const zodValidator: () => {
    validate({ value }: {
        value: unknown;
    }, fn: ZodType): ValidationError;
    validateAsync({ value }: {
        value: unknown;
    }, fn: ZodType): Promise<ValidationError>;
};
